[project]
name = "faust-api"
version = "0.1.0"
description = "Backend of the Faust night club site: public menu, atmosphere photos, admin API"
requires-python = ">=3.13"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.32",
  "pydantic>=2.9",
  "pydantic-settings>=2.6",
  "sqlalchemy[asyncio]>=2.0",
  "asyncpg>=0.30",
  "alembic>=1.14",
  "pyjwt>=2.10",
  "bcrypt>=4.2",
  "pillow>=11.0",
  "pillow-heif>=0.21",
  "python-multipart>=0.0.20",
  "httpx>=0.28",
]

[dependency-groups]
dev = [
  "pytest>=8.3",
  "pytest-asyncio>=0.24",
  "ruff>=0.8",
  "mypy>=1.13",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/faust_api"]

[tool.ruff]
line-length = 110
target-version = "py313"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "ASYNC", "RUF"]
# The whole product speaks Ukrainian, so every user-facing string is Cyrillic.
# RUF001-003 warn that "о" is not "o" — true, and useless here: it would fire on
# every error message in the codebase.
ignore = ["RUF001", "RUF002", "RUF003"]

[tool.mypy]
python_version = "3.13"
strict = true
plugins = ["pydantic.mypy"]
files = ["src", "tests"]
warn_unreachable = true

# pillow-heif ships no type information; it is one call — registering the HEIC
# opener — and everything downstream of it is Pillow, which is typed.
[[tool.mypy.overrides]]
module = ["pillow_heif.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
